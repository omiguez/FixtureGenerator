using System;
using Xunit;

namespace FixtureGenerator.Tests
{
    public class FixtureGenerationTest
    {
        /// Things to test:
        /// - Value generation for each type, including lists, dictionaries, and classes
        /// - Make sure generation is the same with the same seed
        /// - Tree generation
        /// - Common tree generation
        /// - Tree value creation
        /// - Value asignation with common elements
    }
}
